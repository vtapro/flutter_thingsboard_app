name: greeniq_customizations
description: Local plugin with Greeniq custom UI helpers for ThingsBoard app.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

